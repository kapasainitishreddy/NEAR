# Receipts has NO backend and requires NO environment variables for the MVP.
# Everything runs locally in the browser using on-device storage.
#
# This file exists only as a placeholder. Do NOT add real secrets, API keys,
# or tokens here — and never commit a real .env file (it is gitignored).
